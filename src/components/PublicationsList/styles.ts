import styled from "styled-components";

export const PublicationsListContainer = styled.main`
  width: 80%;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  margin: 0 auto;
  padding: 10px;
  gap: 1rem;
`;
